package solution

// SpinSum конкурентно суммирует инкременты под защитой самодельного спинлока на
// sync/atomic.
//
// Запусти goroutines горутин; каждая perGoroutine раз увеличивает общий счётчик
// на 1. Инкремент выполняется только внутри критической секции, которую охраняет
// спинлок: int32, где 0 — свободно, 1 — занято. Захват — это CAS(0 → 1) в цикле,
// отпускание — Store(0). Дождись всех горутин через sync.WaitGroup и верни счётчик.
//
// Результат детерминирован и равен goroutines * perGoroutine.
//
// Пример: SpinSum(4, 250) → 1000; SpinSum(0, 100) → 0.
func SpinSum(goroutines, perGoroutine int) int {
	// твоя реализация
	return 0
}
