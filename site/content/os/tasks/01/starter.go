package solution

// Schedule моделирует Round Robin и возвращает потактовый таймлайн: на каждую
// единицу времени — имя задачи, которая занимала CPU в этот момент.
//
// Правила:
//   - все задачи готовы с момента 0 и обслуживаются по кругу в порядке, в
//     котором они переданы;
//   - каждая задача получает не больше quantum единиц подряд, затем уходит в
//     конец очереди (если ей ещё осталось считать);
//   - quantum >= 1.
//
// Пример: jobs = [{A,3},{B,3}], quantum = 1  →  [A B A B A B]
func Schedule(jobs []Job, quantum int) []string {
	// твоя реализация
	return nil
}
