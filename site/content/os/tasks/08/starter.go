package solution

// HasDeadlock определяет, есть ли взаимоблокировка, по графу ожидания
// (wait-for graph).
//
// waitsFor задаёт ориентированный граф: ключ — id потока, значение — список
// потоков, которых он ждёт. Ребро A → B читается как «поток A ждёт поток B».
//
// Дедлок существует тогда и только тогда, когда в графе есть ориентированный
// цикл (включая самопетлю A → A — поток ждёт сам себя).
//
// Пример:
//
//	HasDeadlock(map[int][]int{1: {2}, 2: {1}})        → true
//	HasDeadlock(map[int][]int{1: {2}, 2: {3}, 3: {}}) → false
func HasDeadlock(waitsFor map[int][]int) bool {
	// твоя реализация
	return false
}
