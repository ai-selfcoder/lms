package solution

// LRUMisses считает число промахов страниц (page faults) при политике
// вытеснения LRU (Least Recently Used).
//
// Параметры:
//   - refs — поток обращений к страницам (их номера по порядку);
//   - capacity — сколько страниц помещается в кэше (число кадров).
//
// Промах — это обращение к странице, которой нет в кэше (включая первые
// загрузки в пустой кэш). При промахе и полном кэше вытесняется страница,
// к которой дольше всего не обращались. Любое обращение делает страницу
// «самой свежей».
//
// Краевые случаи:
//   - capacity <= 0: кэшировать негде, каждое обращение — промах (вернуть len(refs));
//   - пустой refs: вернуть 0.
//
// Пример: LRUMisses([]int{1, 1, 1, 1}, 2) → 1
func LRUMisses(refs []int, capacity int) int {
	// твоя реализация
	return 0
}
