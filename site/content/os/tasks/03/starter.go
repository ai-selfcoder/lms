package solution

// Translate выполняет аппаратную трансляцию виртуального адреса в физический по
// схеме base-and-bounds (динамическое перемещение).
//
// Если виртуальный адрес лежит в пределах процесса (0 <= va < bound), функция
// возвращает физический адрес base+va и true. Иначе — обращение вне границ
// (segmentation fault): возвращается 0 и false.
//
// Параметры:
//   - va    — виртуальный адрес, с которым работает процесс (начинается с 0);
//   - base  — физический адрес начала процесса;
//   - bound — размер области процесса (валидны индексы 0 .. bound-1).
//
// Пример: Translate(100, 32768, 16384) → 32868, true
//
//	Translate(16384, 32768, 16384) → 0, false  (va == bound, уже вне)
func Translate(va, base, bound int) (int, bool) {
	// твоя реализация
	return 0, false
}
