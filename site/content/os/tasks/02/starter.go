package solution

// AvgTurnaround возвращает среднее время оборота (turnaround) при планировании
// по SJF (Shortest Job First, без вытеснения). Все задачи готовы с момента 0.
//
// Время оборота задачи = момент её завершения (приход = 0). SJF выполняет задачи
// в порядке возрастания Burst. Для пустого среза верни 0.
//
// Пример: [{A,5},{B,3},{C,1}] → порядок C,B,A, завершения 1,4,9 → (1+4+9)/3.
func AvgTurnaround(jobs []Job) float64 {
	// твоя реализация
	return 0
}
